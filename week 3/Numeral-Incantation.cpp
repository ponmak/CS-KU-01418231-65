#include<iostream>

using namespace std;

typedef struct Node {
    int data;
    Node* next;
} Node;


int main(){

    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n;
    cin >> n;

    Node* head = new Node();

    for (int i = 0; i < n; i++){

    }
    return 0;
}